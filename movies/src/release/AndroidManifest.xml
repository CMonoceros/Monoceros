<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.zjmzyx.monoceros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/movies_ic_launcher"
        android:label="@string/movies_app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/movies_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/movies_AppTheme">

    </application>
</manifest>
